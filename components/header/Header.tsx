import React from 'react';
import { Container, Title } from './Header.styles';

const Header = () => {
  return (
    <Container>
      <Title>Header</Title>
    </Container>
  );
};

export default Header;
